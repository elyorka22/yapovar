# Деплой на Railway

Инструкция по развертыванию проекта MEN OSHPAZ на Railway.

## Подготовка

1. Убедитесь, что у вас есть:
   - Аккаунт на [Railway](https://railway.app)
   - GitHub репозиторий с проектом (или используйте Railway CLI)
   - Токен Telegram бота от @BotFather

## Шаг 1: Создание проекта на Railway

### Вариант A: Через веб-интерфейс

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo" (если проект на GitHub)
   - Или "Empty Project" для загрузки через CLI
4. Выберите ваш репозиторий

### Вариант B: Через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init

# Создайте новый проект
railway new
```

## Шаг 2: Настройка переменных окружения

В настройках проекта Railway добавьте следующие переменные:

### Обязательные переменные:

```
BOT_TOKEN=ваш_токен_от_BotFather
PORT=3000
NODE_ENV=production
ADMIN_CHAT_ID=ваш_telegram_chat_id
```

**Или для нескольких админов:**
```
ADMIN_CHAT_IDS=123456789,987654321,111222333
```

**Примечание:** Если `ADMIN_CHAT_ID` или `ADMIN_CHAT_IDS` не указаны, команда `/admin` будет доступна всем (только для разработки!).

**Как получить ADMIN_CHAT_ID:**
1. Напишите боту @userinfobot в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш Chat ID
4. Добавьте в переменные окружения Railway

### Опциональные переменные:

```
WEBAPP_URL=https://ваш-домен.railway.app
RAILWAY_PUBLIC_DOMAIN=ваш-домен.railway.app
```

### Как добавить переменные:

1. В проекте Railway перейдите в "Variables"
2. Нажмите "New Variable"
3. Добавьте каждую переменную по отдельности

Или через CLI:

```bash
railway variables set BOT_TOKEN=ваш_токен
railway variables set PORT=3000
railway variables set NODE_ENV=production
```

## Шаг 3: Настройка домена

1. В проекте Railway перейдите в "Settings"
2. Найдите раздел "Domains"
3. Нажмите "Generate Domain" или добавьте свой кастомный домен
4. Скопируйте полученный домен (например: `your-project.railway.app`)
5. Обновите переменную `WEBAPP_URL`:
   ```
   WEBAPP_URL=https://your-project.railway.app
   ```

## Шаг 4: Деплой

### Автоматический деплой (через GitHub)

1. Railway автоматически задеплоит проект при push в репозиторий
2. Деплой начнется автоматически после настройки переменных окружения

### Ручной деплой (через CLI)

```bash
# Задеплойте проект
railway up

# Или через git
git push railway main
```

## Шаг 5: Проверка работы

1. Проверьте логи в Railway Dashboard → "Deployments" → выберите последний деплой → "View Logs"
2. Убедитесь, что видите:
   ```
   Server running on port 3000
   WebApp URL: https://your-project.railway.app
   Bot is running...
   ```
3. Откройте ваш Telegram бот и отправьте `/start`
4. Проверьте WebApp по адресу: `https://your-project.railway.app`

## Настройка Telegram WebApp

1. Откройте @BotFather в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Нажмите "Configure Menu Button"
6. Введите URL: `https://your-project.railway.app`
7. Введите текст кнопки: "Открыть магазин" или "🛒 Магазин"

## Структура проекта на Railway

```
/
├── bot.js              # Основной файл бота и сервера
├── index.html          # Главная страница
├── products.html       # Страница товаров
├── admin.html          # Админ-панель
├── styles.css          # Стили
├── script.js           # JavaScript для сайта
├── products.js         # JavaScript для товаров
├── admin.js            # JavaScript для админ-панели
├── admin.css           # Стили админ-панели
├── package.json        # Зависимости
├── railway.json        # Конфигурация Railway
├── Procfile            # Команда запуска
└── .railwayignore      # Игнорируемые файлы
```

## Мониторинг и логи

### Просмотр логов:

1. В Railway Dashboard → "Deployments" → выберите деплой → "View Logs"
2. Или через CLI:
   ```bash
   railway logs
   ```

### Метрики:

- Railway автоматически отслеживает:
  - Использование CPU и памяти
  - Количество запросов
  - Время ответа

## Обновление проекта

### Автоматическое обновление:

Просто сделайте `git push` в ваш репозиторий, Railway автоматически задеплоит изменения.

### Ручное обновление:

```bash
railway up
```

## Решение проблем

### Бот не отвечает:

1. Проверьте, что `BOT_TOKEN` установлен правильно
2. Проверьте логи на наличие ошибок
3. Убедитесь, что бот запущен (в логах должно быть "Bot is running...")

### WebApp не открывается:

1. Проверьте, что `WEBAPP_URL` установлен правильно
2. Убедитесь, что домен настроен в Railway
3. Проверьте, что сервер запущен на правильном порту

### Ошибки при деплое:

1. Проверьте логи деплоя в Railway Dashboard
2. Убедитесь, что все зависимости указаны в `package.json`
3. Проверьте версию Node.js (должна быть >= 18.0.0)

## Полезные команды Railway CLI

```bash
# Просмотр статуса
railway status

# Просмотр логов
railway logs

# Открыть проект в браузере
railway open

# Просмотр переменных окружения
railway variables

# Установить переменную
railway variables set KEY=value

# Удалить переменную
railway variables unset KEY
```

## Стоимость

Railway предоставляет:
- **$5 бесплатных кредитов** каждый месяц
- После исчерпания: **$0.000463/GB RAM в час**

Для этого проекта обычно достаточно бесплатных кредитов.

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию Railway: https://docs.railway.app

