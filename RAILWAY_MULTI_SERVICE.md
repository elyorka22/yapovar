# Настройка трех сервисов в Railway

Проект настроен для работы как три отдельных сервиса в одном проекте Railway:

1. **Backend + Frontend** (`server.js`) - Express сервер с API и статическими файлами
2. **Bot** (`bot-service.js`) - Telegram бот
3. **Legacy** (`bot.js`) - Монолитный вариант (для совместимости)

## 🚀 Деплой на Railway

### Вариант 1: Автоматический (рекомендуется)

Railway автоматически определит сервисы по `railway.toml` или `railway-services.json`.

1. Создайте проект на Railway
2. Подключите GitHub репозиторий
3. Railway создаст два сервиса автоматически:
   - `backend-frontend` - запустит `node server.js`
   - `bot` - запустит `node bot-service.js`

### Вариант 2: Ручная настройка

1. Создайте проект на Railway
2. Подключите GitHub репозиторий
3. В настройках проекта добавьте **новый сервис**:
   - Нажмите "+ New" → "GitHub Repo"
   - Выберите тот же репозиторий
   - В настройках сервиса установите:
     - **Name**: `backend-frontend`
     - **Start Command**: `node server.js`
     - **Healthcheck Path**: `/health`
4. Добавьте еще один сервис:
   - **Name**: `bot`
   - **Start Command**: `node bot-service.js`

## 📋 Переменные окружения

Установите переменные окружения для **каждого сервиса**:

### Backend + Frontend сервис:
```
PORT=3000
NODE_ENV=production
ADMIN_CHAT_ID=ваш_telegram_chat_id
ADMIN_CHAT_IDS=123456789,987654321
WEBAPP_URL=https://ваш-проект.railway.app
RAILWAY_PUBLIC_DOMAIN=ваш-проект.railway.app
```

### Bot сервис:
```
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=https://ваш-проект.railway.app
RAILWAY_PUBLIC_DOMAIN=ваш-проект.railway.app
ADMIN_CHAT_ID=ваш_telegram_chat_id
ADMIN_CHAT_IDS=123456789,987654321
```

**Примечание:** Можно установить переменные на уровне проекта, и они будут доступны всем сервисам.

## 🔄 Как работают сервисы

### Backend + Frontend (`server.js`)
- Обрабатывает HTTP запросы
- Отдает статические файлы (HTML, CSS, JS)
- Предоставляет API endpoints:
  - `/api/products`
  - `/api/banners`
  - `/api/hero`
  - `/api/orders`
  - `/api/check-admin`
- Health check: `/health`

### Bot (`bot-service.js`)
- Обрабатывает Telegram команды и сообщения
- Отправляет уведомления пользователям
- Отправляет заказы администратору
- Мониторит `orders.json` для новых заказов
- Мониторит изменения статусов заказов

### Общие файлы
Оба сервиса используют общие JSON файлы:
- `products.json`
- `banners.json`
- `hero.json`
- `orders.json`
- `bot-settings.json`

В Railway все сервисы в одном проекте имеют доступ к одной файловой системе.

## 🔍 Проверка работы

### Backend + Frontend:
```bash
curl https://ваш-проект.railway.app/health
# Должен вернуть: {"status":"ok","service":"backend-frontend",...}
```

### Bot:
- Отправьте `/start` боту в Telegram
- Бот должен ответить

## 📊 Мониторинг

В Railway Dashboard вы увидите:
- Два отдельных сервиса
- Логи каждого сервиса отдельно
- Метрики каждого сервиса
- Возможность перезапустить каждый сервис отдельно

## 🆘 Решение проблем

### Сервисы не запускаются
- Проверьте логи в Railway Dashboard
- Убедитесь, что переменные окружения установлены
- Проверьте, что команды запуска правильные

### Бот не отвечает
- Проверьте логи сервиса `bot`
- Убедитесь, что `BOT_TOKEN` установлен
- Проверьте, что бот не заблокирован

### API не работает
- Проверьте логи сервиса `backend-frontend`
- Убедитесь, что сервис запущен (проверьте `/health`)
- Проверьте переменные окружения

## ✅ Преимущества разделения

1. **Масштабирование**: Можно масштабировать каждый сервис отдельно
2. **Надежность**: Если один сервис упадет, другой продолжит работать
3. **Мониторинг**: Легче отслеживать проблемы в конкретном сервисе
4. **Разработка**: Можно разрабатывать и деплоить сервисы независимо

