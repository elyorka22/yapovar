# 🚂 Настройка нескольких сервисов в Railway

Railway **не создает автоматически** несколько сервисов из одного репозитория. Нужно создать их **вручную**.

## 📋 Пошаговая инструкция

### Шаг 1: Создайте первый сервис (Backend + Frontend)

1. В Railway Dashboard откройте ваш проект
2. Нажмите **"+ New"** → **"Empty Service"** (или **"GitHub Repo"**)
3. Если выбираете GitHub Repo:
   - Выберите репозиторий `elyorka22/yapovar`
   - Railway создаст сервис автоматически
4. В настройках сервиса:
   - **Name**: `backend-frontend` (или любое другое имя)
   - **Start Command**: `node server.js`
   - **Healthcheck Path**: `/health` (опционально)

### Шаг 2: Создайте второй сервис (Bot)

1. В том же проекте нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите **тот же репозиторий** `elyorka22/yapovar`
3. В настройках сервиса:
   - **Name**: `bot` (или любое другое имя)
   - **Start Command**: `node bot-service.js`
   - **Healthcheck Path**: оставьте пустым (бот не имеет HTTP endpoints)

### Шаг 3: Настройте переменные окружения

#### Вариант 1: На уровне проекта (рекомендуется)

1. В Railway Dashboard откройте **Settings** → **Variables**
2. Добавьте переменные (они будут доступны всем сервисам):

```
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_CHAT_ID=ваш_telegram_chat_id
ADMIN_CHAT_IDS=123456789,987654321
NODE_ENV=production
WEBAPP_URL=https://ваш-проект.railway.app
RAILWAY_PUBLIC_DOMAIN=ваш-проект.railway.app
```

#### Вариант 2: Для каждого сервиса отдельно

**Backend + Frontend сервис:**
```
PORT=3000
NODE_ENV=production
ADMIN_CHAT_ID=ваш_telegram_chat_id
ADMIN_CHAT_IDS=123456789,987654321
WEBAPP_URL=https://ваш-проект.railway.app
RAILWAY_PUBLIC_DOMAIN=ваш-проект.railway.app
```

**Bot сервис:**
```
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=https://ваш-проект.railway.app
RAILWAY_PUBLIC_DOMAIN=ваш-проект.railway.app
ADMIN_CHAT_ID=ваш_telegram_chat_id
ADMIN_CHAT_IDS=123456789,987654321
```

### Шаг 4: Получите публичный URL

1. В сервисе `backend-frontend` откройте **Settings** → **Networking**
2. Нажмите **"Generate Domain"** или используйте существующий
3. Скопируйте URL (например: `https://ваш-проект.railway.app`)
4. Обновите переменную `WEBAPP_URL` этим значением

### Шаг 5: Проверка работы

#### Проверка Backend + Frontend:
```bash
curl https://ваш-проект.railway.app/health
# Должен вернуть: {"status":"ok","service":"backend-frontend",...}
```

#### Проверка Bot:
- Отправьте `/start` боту в Telegram
- Бот должен ответить

## 🎯 Альтернативный способ (один сервис с переменной)

Если не хотите создавать два сервиса, можно использовать один сервис с переменной `SERVICE_TYPE`:

1. В Railway Dashboard откройте сервис
2. В настройках установите:
   - **Start Command**: `node start.js`
   - **Variables**: `SERVICE_TYPE=backend-frontend` (или `bot`)

Но это **не рекомендуется**, так как теряются преимущества разделения.

## 📊 Структура проекта в Railway

После настройки у вас будет:

```
Проект: yapovar
├── Сервис 1: backend-frontend
│   ├── Start Command: node server.js
│   ├── Healthcheck: /health
│   └── Публичный URL: https://ваш-проект.railway.app
│
└── Сервис 2: bot
    ├── Start Command: node bot-service.js
    └── Healthcheck: (нет)
```

## 🔍 Мониторинг

В Railway Dashboard вы увидите:
- Два отдельных сервиса в списке
- Логи каждого сервиса отдельно
- Метрики каждого сервиса (CPU, Memory, Network)
- Возможность перезапустить каждый сервис отдельно

## 🆘 Решение проблем

### Сервис не запускается
- Проверьте логи в Railway Dashboard
- Убедитесь, что команда запуска правильная
- Проверьте переменные окружения

### Бот не отвечает
- Проверьте логи сервиса `bot`
- Убедитесь, что `BOT_TOKEN` установлен
- Проверьте, что бот не заблокирован

### API не работает
- Проверьте логи сервиса `backend-frontend`
- Убедитесь, что сервис запущен (проверьте `/health`)
- Проверьте публичный URL

### Заказы не приходят админу
- Убедитесь, что оба сервиса запущены
- Проверьте, что `ADMIN_CHAT_ID` установлен
- Проверьте логи обоих сервисов

## ✅ Готово!

Теперь у вас два независимых сервиса, которые могут масштабироваться отдельно.

